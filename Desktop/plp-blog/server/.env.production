

# Database
MONGODB_URI=mongodb+srv://sacredlyula:I1XPaNqP5klIToRP@blog-assignment.fl75lfr.mongodb.net/blog-db?retryWrites=true&w=majority&appName=blog-assignment

# JWT Secret (Generate a strong secret)
JWT_SECRET=your_super_secure_jwt_secret_key_here_make_it_long_and_random

# Port (will be set automatically by most platforms)
PORT=5000

# Frontend URL (update with your deployed frontend URL)
FRONTEND_URL=https://your-frontend-app.vercel.app

# Node Environment
NODE_ENV=production
